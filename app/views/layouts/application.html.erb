<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= generate_title(yield :title) %></title>
    <%= favicon_link_tag "favicon.ico" %>
    <%= render 'layouts/import' %>
  </head>
  <body>
    <div class="container-fluid">
      <% if false %>
        <div id="image-background"></div>
      <% end %>
      <%= video_tag "background.mp4", id: "video-backround", autoplay: true,
        loop: true, muted: true, poster: "slide3.jpg" %>
      <div class="cover-container d-flex h-100 p-3 mx-auto flex-column">
        <header class="masthead mb-auto">

          <div class="inner">
            <h2 class="masthead-brand">John Harvey</h2>
            <nav class="nav nav-masthead justify-content-center home-nav">
              <%= nav_generate "nav-link home-nav-item", :span %>
              <div class="authen-area">
              <%= user_log_status "btn btn-sm btn-outline-secondary blog-in" %>
            </div>
            </nav>

          </div>
        </header>
        <%= render "shared/flash" %>
        <main role="main" class="inner cover">
          <%= yield %>
        </main>

        <footer class="mastfoot mt-auto">
          <div class="inner">
            <p><%= copyright "Lê Chí Nguyên", "All rights reserved" %></p>
          </div>
        </footer>
        <%= render "shared/alert" %>
      </div>
    </div>
  </body>
</html>
