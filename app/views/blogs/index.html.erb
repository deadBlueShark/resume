<% provide :title, "Blogs" %>
<div class="col-md-8 blog-main">
  <h3 class="pb-3 mb-4 font-italic border-bottom">
    From the Firehose
  </h3>
  <%= render @blogs %>
  <%= paginate @blogs %>
</div>

<%= link_to 'New Blog', new_blog_path if logged_in?(:admin) if false %>
